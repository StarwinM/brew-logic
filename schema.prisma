generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Drink {
  id          Int    @id @default(autoincrement())
  name        String @unique
  amount      Int
  description String
}

model Type {
  id   Int    @id @default(autoincrement())
  name String @unique
}
